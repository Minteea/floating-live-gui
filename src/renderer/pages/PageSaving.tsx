import SavingSettings from '../widgets/saving/SavingSettings';

const PageSaving: React.FC = function () {
  return (
    <div>
      <h2>弹幕保存</h2>
      <SavingSettings />
    </div>
  );
};

export default PageSaving;
